import React from "react";

export default function MobileSlider()  {
    // get the slider from interface
    // 考虑课扩容性
    // 在移动端的流畅已经兼容性
    // 可扩容性

    return (
        <div>
            this is mobile slider
        </div>
    )
}